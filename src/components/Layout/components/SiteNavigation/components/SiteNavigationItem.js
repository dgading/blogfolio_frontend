import React from 'react';
import { Link } from 'react-router-dom'

export const SiteNavigationItem = (props) => (
  {/*<div className={"site-navigation__item site-navigation__item--" + (props.title).toLowerCase()}>
    <Link to={"/" + (props.title).toLowerCase()}>{props.title}</Link>
  </div>*/}
);

export default SiteNavigationItem;